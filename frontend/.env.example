# Copy to .env.local and adjust.
#
# Base URL of the API the SPA talks to. Leave it unset to fall back to the
# deployed API (https://hiring-platform-api-h3bz.onrender.com).
VITE_API_URL=http://localhost:3000
