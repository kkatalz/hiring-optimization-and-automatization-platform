export enum VacancySubmissionStatus {
  pending = 'pending',
  interviewing = 'interviewing',
  approved = 'approved',
  rejected = 'rejected',
}

export enum InterviewStatus {
  scheduled = 'scheduled',
  completed = 'completed',
  canceled = 'canceled',
}
